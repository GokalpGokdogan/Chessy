// const express = require('express');
// const router = express.Router();
// const { eval } = require('../controllers/fishController');

// //const requireAuth = require('../middleware/requireAuth');

// // router.use(requireAuth);

// router.get('/', eval);

// module.exports = router;